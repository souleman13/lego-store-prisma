import React, {Component} from 'react'
import Product from './product'

export default class extends Component {
    render(){
        return(
            <section>
                <Product />
            </section>
        )
    }
}